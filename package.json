{
  "name": "nuxtstarterkit-v2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:web": "dotenv -- turbo run dev --filter web",
    "dev:prepare": "dotenv -- turbo run dev:prepare",
    "build": "turbo run build",
    "build:web": "dotenv -- turbo run build --filter web",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "lint:fix": "turbo run lint:fix",
    "postinstall": "pnpm run dev:prepare",
    "prepare": "husky",
    "db:studio": "dotenv -- turbo run db:studio --filter layer-auth",
    "db:generate": "dotenv -- turbo run db:generate --filter layer-auth",
    "db:migrate": "dotenv -- turbo run db:migrate --filter layer-auth",
    "test:e2e": "dotenv -- turbo run test:e2e --filter web",
    "test:e2e:ui": "dotenv -- turbo run test:e2e:ui --filter web"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.1",
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.7.5"
  },
  "dependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
